{
  "name": "sobhan-academy-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:app": "node ../scripts/build-econ-app.mjs",
    "check:app": "node ../scripts/build-econ-app.mjs --check"
  },
  "dependencies": {
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
