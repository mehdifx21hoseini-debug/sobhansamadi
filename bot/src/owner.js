// چه کسانی مدیرند.
//
// این فهرست پیش‌تر یک عدد بود که در سه فایل تکرار شده بود. تکرارِ یک
// ثابت یعنی روزی که عوض شود، یکی از سه‌تا جا می‌ماند و آن یکی مسیر
// بی‌صدا برای مدیر بسته می‌شود.

// آیدی عددی - شکل مطمئن. عدد تلگرام هرگز عوض نمی‌شود و به کسی
// منتقل نمی‌شود.
export const OWNER_IDS = ["6923823275"];

// نام کاربری - شکل راحت، ولی ضعیف‌تر.
//
// چرا اصلاً لازم است: ربات نمی‌تواند نام کاربری را به عدد تبدیل کند
// مگر آن شخص قبلاً به ربات پیام داده باشد. تا آن لحظه، تنها راهِ دادن
// دسترسی به کسی که فقط نامش را داریم، همین است.
//
// چرا ضعیف‌تر: نام کاربری در تلگرام قابل تغییر و رها کردن است. اگر
// صاحبش روزی آن را عوض کند، هرکس دیگری می‌تواند همان نام را بردارد و
// با آن مدیر شود. پس این فهرست موقتی است: به‌محض اینکه آن شخص یک‌بار
// /diag بزند، عدد خودش را می‌بیند و باید همان عدد به OWNER_IDS اضافه و
// نامش از اینجا برداشته شود.
//
// بدون @ و با حروف کوچک نوشته می‌شوند؛ مقایسه هم بی‌توجه به بزرگی و
// کوچکی حروف است، چون تلگرام نام‌ها را همین‌طور یکسان می‌بیند.
export const OWNER_USERNAMES = ["sbhnsmdd"];

// اولین آیدی عددی: مقصد پیام‌های خودکار ربات به مدیر. نام کاربری
// اینجا به کار نمی‌آید، چون برای فرستادن پیام باید عدد داشت.
export const OWNER_ID = OWNER_IDS[0];

export function isOwner(ctx) {
  const from = ctx && ctx.from;
  if (!from) return false;
  if (OWNER_IDS.includes(String(from.id))) return true;
  const username = String(from.username || "").toLowerCase();
  return !!username && OWNER_USERNAMES.includes(username);
}
